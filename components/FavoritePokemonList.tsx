import { Text } from 'react-native';

// ['ivysaur', 'charmeleon']
// loop through the array
// for each name call the api: https://pokeapi.co/api/v2/pokemon/${name}
// add the result of the call to some array, probably in state
// render the array of objects fetched from the API with flat list

export default function FavoritePokemonList() {
	return <Text>favorite pokemon</Text>;
}
